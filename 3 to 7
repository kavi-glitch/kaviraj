-------------3rd-----------------


-- INSERT
INSERT INTO Employees VALUES
(4,'David','Wilson','david.wilson@company.com','2023-08-10',2);

-- DELETE
DELETE FROM Employees WHERE employee_id=2;

-- ALTER
ALTER TABLE Employees ADD phone_number VARCHAR(20);
ALTER TABLE Employees MODIFY phone_number VARCHAR(50);
ALTER TABLE Employees RENAME COLUMN phone_number TO contact_number;
ALTER TABLE Employees DROP COLUMN contact_number;

-- UPDATE
UPDATE Employees 
SET email='charlie.brown@newemail.com'
WHERE employee_id=3;

-- SELECT
SELECT * FROM Employees WHERE department_id=2;
SELECT * FROM Employees WHERE hire_date>'2021-01-01';
SELECT * FROM Employees WHERE first_name LIKE 'A%';

SELECT e.first_name,e.last_name,d.department_name
FROM Employees e
JOIN Departments d
ON e.department_id=d.department_id;

-- CONDITIONS
SELECT * FROM Employees 
WHERE department_id=1 AND hire_date>'2022-01-01';

SELECT * FROM Employees 
WHERE department_id=1 OR department_id=3;

SELECT * FROM Employees 
WHERE department_id IN (1,3);

SELECT * FROM Employees 
WHERE hire_date BETWEEN '2021-01-01' AND '2022-01-01';

SELECT * FROM Employees 
WHERE phone_number IS NULL;



-------------7th-----------

-- CREATE DATABASE
CREATE DATABASE mca;

-- CREATE TABLE
CREATE TABLE mca.student_info(
    id INT PRIMARY KEY,
    name VARCHAR(30),
    age INT,
    town VARCHAR(40)
);

-- INSERT
INSERT INTO mca.student_info VALUES
(1,"Surya",22,"Salem"),
(2,"Saran",21,"Salem"),
(3,"Hari",22,"Namakkal"),
(4,"Vijay",21,"Salem"),
(5,"Ajith",22,"Erode"),
(6,"Vikram",21,"Chennai"),
(7,"Nantha",22,"Namakkal"),
(8,"Sathish",21,"Salem");

-- UPDATE
UPDATE mca.student_info
SET town="Salem"
WHERE id=6;

-- DELETE
DELETE FROM mca.student_info
WHERE id=5;

-- READ
SELECT * FROM mca.student_info;
